{
	"routes": [
		{
			"handle": "filesystem"
		},
		{
			"src": "/api/(.*)",
			"dest": "https://mahabazzar-backend.vercel.app/api/$1",
			"methods": ["GET", "POST", "PUT", "PATCH", "DELETE"]
		},
		{
			"src": "/(.*)",
			"dest": "/index.html"
		}
	]
}
